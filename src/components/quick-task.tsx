"use client";import{useState}from"react";import{Plus,X}from"lucide-react";import type{FocusPeriod,Goal,Outcome}from"@/lib/types";import{TaskForm}from"@/components/task-form";export function QuickTask({goals,focus,outcomes}:{goals:Goal[];focus:FocusPeriod[];outcomes:Outcome[]}){const[open,setOpen]=useState(false);return <><button className="quick-add" onClick={()=>setOpen(true)} aria-label="Add task"><Plus size={25}/><span>Task</span></button>{open&&<div className="dialog-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}}><div className="dialog"><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><div><p className="eyebrow" style={{margin:0}}>Quick add</p><h2 style={{margin:".25rem 0 1rem"}}>New task</h2></div><button className="btn btn-ghost" onClick={()=>setOpen(false)}><X/></button></div><TaskForm goals={goals} focus={focus} outcomes={outcomes} compact/></div></div>}</>}
